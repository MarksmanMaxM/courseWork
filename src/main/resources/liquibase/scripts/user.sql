-- liquibase formatted sql

-- changeset mMax:1

/*CREATE TABLE DYNAMIC_RULES
(
    id          SERIAL,
    name        TEXT,
    description TEXT
);

CREATE TABLE RULES_BY_DYNAMIC_RULES
(
    id_dynamic_rule SERIAL,
    name            TEXT,
    id_rule         VARCHAR(36) DEFAULT (UUID())
);*/
